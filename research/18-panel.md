按 用户 → 场景 → 意图 → 需要的信息 → 最优形式 拆。先定用户，再定场景，最后回答「图和主视图区别在哪、图旁边还要放什么」。

**一、这套体系面向谁**

| 用户 | 在 Anatole 里是谁 | 他打开系统时的问题 |
|---|---|---|
| PM | 杨晓帆，高集中、十几只票、深度自下而上 | 我的 thesis 每一环现在是什么状态，该不该动仓位 |
| 研究员 | 覆盖 NBIS / RBRK 的人 | 这周来了什么数、超没超预期、传导到票的哪一环 |
| 数据岗 | 这个岗位本身，深圳数据团队 | 链稳不稳、哪个源过期、哪条边没验证、下次数据什么时候来 |
| 评审者 | 面试官 | 这人怎么想的、每个数从哪来、哪些是 AI 做的 |

第四类是笔试特有的，决定了 P 级和 owner 标记必须在每一行可见，而不是藏在抽屉里。

**二、场景 → 意图 → 信息 → 形式**

| 场景 | 主用户 | 意图 | 必须在同一视图看到 | 最优形式 |
|---|---|---|---|---|
| A 周初例行 / 数据到达 | 研究员 | 什么时候来、来了多少、偏离预期没 | 日期、实测 / 预期 / 上期、P 级、owner、下次发布依据 | 按时间分组的表，就是现在的主视图 |
| B 一个数出来后追问「打到谁」 | 研究员、PM | 传导到书里哪只票、几跳、多确定、是供给还是需求 | 节点最新值、中间机制节点、边的 tier 和跳数、票的权重、供给侧还是需求侧、regime 横带 | 每票一个蝴蝶结 |
| C 投委会前审一只票 | PM | thesis 每环状态、供需对账 regime | 左右两侧节点各自最新值和三态、对账节点结论、哪条边是 ★ 关键 | 同一个蝴蝶结，节点带状态 |
| D 链健康巡检 | 数据岗 | 哪条链断了 | 下次发布已过期未更新、源 stale、placeholder 数、边 last_validated | 状态清单，或在图上给节点上色 |
| E 扩票 / 回测 | 数据岗 | 加实体加边、标定系数 | registry 三张表 | CSV / DDL，不是界面 |

**三、主视图和图的区别**

同一张 metric_registry，两种投影。主视图是按 knowledge_time 投影，一行是一个数据点，回答「when」和「偏离没」。图是按 edge_registry 投影，一个节点是一个指标，边是机制，回答「so what for the book」。场景 A 用前者，场景 B 和 C 用后者。场景 A 到 B 的过渡就是研究员的真实工作流：数出来，先看偏离，再问打到谁。所以两者不是二选一，是一次点击的前后两步。

**四、图旁边必须同屏的信息，以及不该放的**

同屏必须有，否则每个节点都得开抽屉：
- 节点上的最新值加 as-of 加 P 级徽章。
- 节点上的下次数据点日期，没有排期就显示「无排期」。这是把时间轴融进因果轴，是场景 B 的核心增量。
- 边上的跳数、tier 线型、可交易 / 预警两个分数。
- 票的权重和当前 regime 三态。
- 中间机制节点，不能省成一个数字。

不该同屏，留在抽屉：来源链接全文、假设 ID 与状态、边的验证方法、日历条目明细。这些是场景 D 和 E 的信息，塞进图会把场景 B 淹掉。

**五、我的结论**

主区做「日历 / 映射图」切换，右栏 watchlist 点票直接切到该票的蝴蝶结，抽屉共用。场景 D 先不做独立视图，在视图 A 树里把「下次发布已过期」标红即可，工程量小。图从边总表数据驱动，节点值和日期从指标总表和排期表取，三处永远一致。

这样四类用户各有落点：研究员在日历，PM 在图，数据岗在树和 CSV，评审者在每一行的 P 级和 owner。

两个问题分开答。

## 一、数据岗需要看板吗

**不需要 PM 那种看板，需要的是一个状态面，而且它的自然形态是数据库视图，不是界面。**

理由从数据岗的问题形态来：数据岗的问题是集合型的，「哪些指标过期了、哪些源被拦、哪些边没验证、哪些 🟠 假设还没标定」。集合型问题用 SQL 一句就答，用界面反而要点很多次。数据岗的工作对象是 registry 和逐条观测记录，改数据是改表，不是改页面。而且数据岗更需要推送而不是浏览：链断了应该被告警找到，不是每天打开页面找它。

需要界面的只有三种情形，都很薄：
- 每天一眼「有没有东西坏了」，一张过期和阻塞的清单就够，不需要图。
- 给不写 SQL 的人看审计结果，比如 PM 问「这个数为什么还是 8 月的」，能把记录和来源指给他看。
- 这次笔试要把「链稳不稳」作为证据展示给评审者。

**和 PM 看板的区别**

| | PM / 研究员面 | 数据岗面 |
|---|---|---|
| 投影 | 时间轴、因果图 | registry 全集 |
| 问题 | 这条信息打到谁、多确定、下一个盯什么 | 什么坏了、什么没验、什么该复验 |
| 含判断 | 有，regime、影响方向 | 无，只有状态 |
| 形态 | 表加图加影响卡 | 计数器加清单，点开到逐条记录 |
| 产生方式 | 需要设计 | 应直接由视图生成，比如 v_health |

所以我的建议：数据岗面在笔试交付里保留一个由视图生成的健康表，放在看板第三个切换，因为交付物是静态文件。真实工作里它应该是数据库视图加告警，不该再做一套界面。

## 二、现在底层数据怎么管，工具栈是什么

如实说，现在是文件级管理，没有数据库实例。

| 层 | 现状 | doc 17 / 21 的设计 | 差距 |
|---|---|---|---|
| 真源 | 三份 CSV：指标总表 75 行、边总表 23 行、日历排期 58 行 | Postgres 三张表 metric_registry、edge_registry、fct 四类 | 结构在，载体是文件 |
| 观测记录 | 没有独立表，值写在指标总表的 value_latest 里，多事实混一行 | fct_quant、fct_event 等，每条带 knowledge_time、source_id、anchor | 这是最大的缺口，也是图 4 问题的根 |
| 来源 | 来源总账增补，Markdown 表 | source_master 表，fct 外键指向它 | 有字段无外键约束 |
| 假设 | 假设台账增补，Markdown 表 | doc 30 状态机 | 同上 |
| 校验 | 每次写完跑一段 Python，查解析、重复 id、边和日历引用的指标存在、已填行必有源和 P 级 | CHECK 约束、受控词表 | 手动，不是约束 |
| 处理 trace | 无 | Langfuse | step 3 未做 |
| 呈现 | 构建脚本读 CSV 生成 JSON 注入 HTML | 同 | 一致 |
| 版本 | 无，文件夹不是 git 仓库 | append-only 日志 | 日志靠约定，不靠工具 |

工具：Python 的 csv 模块、openpyxl 读 13F 表格、pdftotext 抽 PDF、WebFetch 抽页面、浏览器做渲染验证。这个会话里有 Tiger 的 TimescaleDB 连接可用，但我没用，因为笔试不需要为三张小表起一个云库。

**我的建议，按代价排**
1. 立刻拆观测记录表，一行一个事实。这是修图 4 和影响卡的前提，也是把「一手可回溯」从口号变成结构。
2. 用 DuckDB 直接读这四份 CSV，把 doc 21 的视图写成真 SQL，比如 v_health、v_signal_latest、v_early_signal。零安装，构建脚本改为查视图而不是自己拼。这样交付里能给面试官看到真 SQL 在跑，而 CSV 仍是唯一真源。
3. 把文件夹初始化成 git 仓库，append-only 日志就有了工具保证。这一步改文件夹状态，你点头我再做。
4. Postgres 或 Tiger 留作生产设计，在交付里写清「设计到 DDL，实例化到 DuckDB」，不假装已经上库。

你点到的是定位错配，不是样式问题。我把它拆成三层说：现在问题在哪、这个看板到底为谁、怎么改。

## 一、现在的问题，根因是一个

**根因：一张指标表同时干了三件事。** doc 21 的设计是三张表分工：registry 存节点定义，fct 存逐条观测，edge 存传导。我在填数时为了快，把观测值写成了 value_latest 一段话，把多个事实塞进一行（图 4 那段 Anthropic + OpenAI + xAI 的 hero 文本就是证据）。于是抽屉只能把这一行原样展示，它天然是一张「记录卡」，服务的是维护数据的人。这和 capex 四家挤一行是同一个错误，我在环 14 改了一处，没改到底。

由这个根因派生出你看到的四个症状：

| 症状 | 表现 | 谁受伤 |
|---|---|---|
| 入口出口错配 | 日历是研究员的入口，点进去却是数据岗的抽屉 | 研究员 |
| 跳转无上下文 | 「在映射图中定位」只高亮一个节点，没有按这条信息筛出它的下游子图 | 研究员、PM |
| 一行多事实 | value_latest 是段落，既不能按记录维护，也不能按记录呈现 | 数据岗 |
| 事件型行信息密度低 | Forecast、Prior、三态对事件型指标几乎全空，而研究员真正想看的「打到谁」不在表上 | 研究员 |

## 二、看板为谁：三类需求，两个面，不是一个抽屉

**主需求是研究员和 PM 的决策支持**，链条是固定的四问：这是什么 → 打到我书里谁 → 多确定 → 接下来盯什么。理由：题面是「监测 AI 发展」服务基金决策；面试官说数据岗必须端到端理解分析，不能只管前面；而且面试官评审时会以 PM 视角问「研究员能不能用」。

**次需求是数据岗的链健康**：新鲜度、覆盖、验证状态、源可得性。这是第一考核「链稳不稳」，但它是运维面，不该占据研究入口。

**评审者**读的是诚实标记，P 级和 owner 在两个面上都保留即可。

结论：研究面和数据面必须是两个不同的界面，共享同一份数据，而不是一个抽屉里上半段给研究员、下半段给数据岗。

## 三、怎么改

**数据层先拆，否则界面改不动。** 新建观测记录表，一行一个事实：指标、实体、值、单位、期间、knowledge_time、来源、P 级。指标总表退回纯 registry。图 4 那一行会变成六条记录，可以按日期排、按来源筛、逐条复验。

**研究面：日历行点开变成「影响卡」，四段对应四问。**
1. 这是什么：一句事实加值加 P 级，一行。
2. 打到谁：这条信息的下游子图，本节点 → 中间机制 → 票，票带权重和当前 regime。每条边一行人话：几跳、供给还是需求还是 regime、可交易还是预警占优。
3. 多确定：tier 翻译成一句话，比如「T2 结构成立，Reflection 合同证过一次，不可回归」。
4. 接下来盯什么：同一条链上其他节点的下次数据点按日期排。
底部折叠一个「数据卡」给数据岗，就是现在抽屉的内容。

同时把「打到谁」上表：事件型行的 Forecast 和 Prior 列换成「影响」列，显示受影响票加方向。三态灰点对研究员没有信息，撤掉。

**映射面加聚焦模式。** 从影响卡点「看整条链」过去时，只渲染受影响的票，无关边淡到两成，顶部留面包屑「来自：xAI Series E，2026-01-06」。这样跳转就带着上下文。

**数据面独立成第三个切换「数据健康」。** 一张表：指标、最近观测日、下次到期、是否过期、P 级、验证状态、源可得性、假设状态。点指标展开逐条观测记录。不再有 hero 文本。

**工作量**：拆表加影响卡约四小时，聚焦模式一小时，数据健康面一个半小时，周六内能做完。优先级是拆表和影响卡，因为它们同时修掉四个症状里的三个。

**需要你定的一件事**：影响卡里「多确定」那句人话，我可以从 cert_method 和 evidence 自动拼，但拼出来是模板句。要不要你为 ★★★ 的关键边各手写一句，作为 D 级判读放进边总表？这会是面试官看到的最像研究员判断的东西。

你点到的问题比排版深，我把它想透了再答。

## 一、问题的根

TradingView 的卡能只写「Beta 0.36」就走，是因为标签、值、单位三者都是外部标准：读者在别处学过，值有公认算法，单位明确。我们的面板把**分析师的工作词表**直接当成了**读者的界面词表**。检查一遍现在的字段，它们分三类，没有一类满足 TradingView 那个条件：

| 类 | 例子 | 为什么读不懂 |
|---|---|---|
| 内部缩写 | 供 需 政、本体、书、regime、链 | 是我们讨论时的简称，读者没学过 |
| 派生判断 | 链状态 ↑!、预警 6、领先读 供↑ T1、信念 | 值是我们自己合成的符号，没有外部定义，也看不出怎么算的 |
| 过程标记 | P1、owner B、T2、验证点、盯、假设 A11 | 是方法论的审计痕迹，对使用者是噪音 |

再加上两个次生问题：值是叙事不是字段，「书里 51.22% 的第一大仓」应该是「持仓 51.22%」加一个日期；以及没有更新时间，读者不知道看到的是什么时候的数。

## 二、解决原则

**原则一，三层词表分离。** 用户层只用标准金融语言和事实：持仓权重、收入、ARR、指引、合同、日期、来源机构。分析层是我们的派生信号，必须用「回答了什么问题」的白话表述，不用代号。方法层是 P 级、owner、tier、假设编号，默认不出现在面板，只在数据卡和悬停里。评审者要看方法层，去数据卡；研究员默认看不到。

**原则二，标签能独立读懂，值带单位和日期。** 检验标准是：把这一行单独截出来给一个没看过我们文档的研究员，他能否读懂。读不懂的，要么改名，要么降级到方法层。任何符号必须有文字对照，↑ 后面跟「加强」。

**原则三，事实作字段，判断作句子，两者分开标。** 字段是可核的：持仓 51.22%，来源 13F，截止 2026-06-30。判断是有人负责的：「供需同向，偏多」，标作者和日期。不混在一段话里。

**原则四，每个值带更新时间，面板顶部带整体更新时间。** 像 TradingView 的「Last update at Sep 19, 07:59」。我们每条记录本来就有 knowledge_time，只是没显示。静态文件阶段写「数据更新至 2026-09-19」，生产阶段就是构建时间。

**原则五，工程上做一张术语表。** `内部名 → 界面名 → 一句定义 → 默认显示层`。界面从术语表取标签，方法论文档继续用内部名。改名只改一张表，这和我们的受控词表原则一致。

## 三、逐项翻译，以 NBIS 对象面板为例

| 现在 | 改成 | 层 |
|---|---|---|
| 书里 51.22% 的第一大仓 | 持仓权重 51.22% · 13F 2026Q2 · 截止 2026-06-30 | 用户 |
| 收入因子 = 产能执行 × 需求持续性 | 我们看什么：数据中心能否按时通电，客户是否持续签约 | 分析，白话 |
| 供 ↑ 需 ↑! 政 ! | 上游供给：加强 · 下游需求：加强，有风险提示 · 政策环境：有变化待判断。每项悬停显示「近 60 天 N 条观测，N 条正面」 | 分析 |
| regime 信念（P5，owner D） | 供需判断：同向偏多 · 分析师判断 · 2026-09-19 | 分析，标作者日期 |
| 预警 6 | 风险提示 6 条，点开是六句话 | 分析 |
| 本周 本体 ↑ | 本周更新：公司自身数据 1 条 | 用户 |
| 领先读 供↑ 需↑ T1 | 领先指标：供给侧向好、需求侧向好 · 可回测 | 分析 |
| 验证点 39 天 | 下一个关键数据：10-28 云厂商三季报资本开支（日期为估计） | 用户 |
| 盯 | 关注 | 用户 |
| thesis 简报一段话 | 拆成字段：持有逻辑一句话；供给端证据三条，每条带数和日期；需求端证据；风险；下一个关键数据 | 分析 |
| P1 徽章 | 来源：公司公告 / 交易所 / 一手媒体，文字不用代号；P 级进数据卡 | 方法 |
| owner B | 不显示；数据卡内 | 方法 |
| T1 T2 T3 | 可回测 / 结构成立 / 仅方向 | 分析 |
| 可交易 0.32 / 预警 0.16 | 不显示数字；只显示「偏可交易」或「偏预警」；数字进数据卡 | 方法 |
| 2 跳 · E3 | 经两步传导 · 客户需求 | 分析 |
| 假设 A11、C18 | 不显示；数据卡内 | 方法 |

信号面板同理：「打到谁」改「影响持仓」；「多确定」改「可信程度」加一句白话；「同链下次数据点」改「接下来的相关数据」；角色标签「领先 / 本体 / regime」改「领先指标 / 公司数据 / 政策」。Watchlist 表头「供 需 政」改全词，右栏宽度够。

## 四、示例：改后的 NBIS 面板顶部

```
NBIS  Nebius Group · AI 云 · 持仓 51.22%
数据更新至 2026-09-19

上游供给   加强        近 60 天 9 条观测，8 条正面
下游需求   加强，有风险提示   6 条观测，含 NVIDIA 关联融资 2 条
政策环境   有变化待判断    BIS 放宽许可 + 25% 关税，尚未反映在 NVIDIA 指引
供需判断   同向偏多 · 分析师判断 · 09-19

持有逻辑   收入靠合同预付，看两件事：数据中心按时通电，客户持续签约
供给端     合同电力目标 5 GW，并网 800MW–1GW（年底目标，08-12）
           客户承诺 >$40B，2026 预付 >$9B（08-12）
           中际旭创上半年营收 +182%（08-22）
需求端     Reflection 融资 $2B 后签 $1B 合同（07-14）
           三大 lab 2026 融资 ≈$207B
风险       NVIDIA 同时是供应商、股东（$2B）和客户的领投方（03-11）
           口径：合同电力 ≠ 并网电力，客户承诺 ≠ 财报 backlog
下一个关键数据   10-28 云厂商三季报资本开支（估计）· 11-10 Nebius Q3（估计）· 12-31 并网目标（公司公告）
```

## 五、需要你定的

术语翻译是命名权，属于 D。上面那张翻译表你过一遍，改你觉得不对的词，我据此建术语表并全站替换。三个我拿不准的：「供需判断」还是「综合判断」；「风险提示」还是「预警」；「领先指标」这个词在基金内部是否已有既定用法。

两问分开答。

## 一、以 PM 和研究员的眼光看改后的面板

**结论：简洁够了，专业还差三处，清晰差一处。**

| 问题 | 为什么 PM 会挑 | 改法 |
|---|---|---|
| 报的是水平，不是变化 | 「合同电力目标 5 GW」PM 早知道。他要的是「4 GW → 5 GW，08-12 上调」。股价对变化反应，不对水平反应 | 证据表每行加「变化」列：上期值 → 本期值，方向 |
| 「加强」没有基准 | 加强相对什么？相对上季、相对指引、还是相对市场预期？没有基准的形容词在投委会上站不住 | 每个状态词后面跟基准：「加强 · 较 Q1」；有指引的对指引：「ARR $3.0B，全年指引 $7–9B，已完成 33 到 43%」 |
| 缺证伪条件 | PM 对一个判断的第一反应是「什么情况下你错了」。我们有「关注」，但写的是要看什么，不是看到什么就推翻 | 「供需判断：同向偏多」下面直接一行「若 12-31 并网 <800MW 或 Q3 预付 <$4B，判断转中性」。阈值现在是占位，那就写「阈值待定，暂用指引下限」并标假设 |
| 缺价格坐标 | PM 活在价格里。我们的体系没接价格，这是设计选择，但面板得说明，否则他会以为漏了 | 顶部一行「本面板只覆盖基本面与上游数据，不含价格与估值」；持仓行给 13F 市值和股数 |

另外两条小的：证据用四列表，值、变化、日期、来源，比八行句子快；顶部要有一句状态总结，PM 只读第一行，比如「Q2 后供需同向；本季验证点是并网兑现与预付到账」。

## 二、怎么把「AI 发展 base」的增量价值呈现出来

先说什么是增量。常规研究员拿到的是财报、指引、卖方。我们的方法论在四个地方给了他拿不到的东西，每一处在体系里都有对应的机制，不是空话：

| 增量 | 机制来源 | NBIS 上的实例 |
|---|---|---|
| 领先：上游节点比财报早 | 六类边、跳数、lead_time_est、中国侧标签 | 中际旭创 H1 +182%（08-22），A 股季报领先美股算力名约 2 到 3 周，先验 18 天待回测 |
| 交叉验证：供需两端独立数据源对账 | 一条 thesis 两端读、对账节点、T2 真值对账 | Reflection 2025-10 融 $2B，278 天后签 NBIS $1B（07-14）。融资到采购这条边被现实证过一次，卖方一般不做 |
| 结构风险：AI 产业链特有的关系 | 循环融资边、regime 边、direction ! | NVIDIA 同时是供芯方、股东（$2B）、客户领投方。$2B 占客户承诺 >$40B 约 5%，计算值 |
| 方向：能力前沿改变需求结构 | 前沿雷达、T3 方向边、两道闸 | Epoch 训练算力 5×/年、开源与闭源差 8 分、每任务成本 $2 到 $8，推理需求弹性向上，只给方向 |
| 证伪管理层 | 口径对照、P 级 | 合同电力 5 GW 不等于并网 800MW–1GW；客户承诺不等于财报 backlog |

**呈现方式：对象面板里加一个固定区块，放在供需状态之后、公司数据之前**，名字暂叫「上游读数」。每行一条边，五列：

| 上游节点 | 最新读数 | 领先 | 对本票含义 | 市场是否已知 |
|---|---|---|---|---|
| 中际旭创 H1 营收 | ¥417.8 亿 +182%（08-22） | 约 2–3 周 | 供给在建强 | 中国侧，海外覆盖少 |
| 四家云厂商 capex | ≈$166B/季，全年指引三家上调 | 同步 | 需求环境强 | 公开 |
| Reflection 融资→签约 | $2B → $1B 合同，278 天 | 约 2 季 | 需求真实性被证一次 | 交叉验证，卖方少做 |
| NVIDIA 投资 Nebius | $2B，占承诺 ≈5% | — | 循环融资敞口 | 公开但少被量化 |
| 前沿训练算力 / 开源差距 | 5×/年；差 8 分 | 数季度 | 推理需求结构向上 | 公开，方向级 |

第五列就是我们 valuable_score 里的独家性因子 e，用词写出来：中国侧、交叉验证、公开。第三列是领先性 l。这样 PM 一眼看出哪几行是别人没有的。

**再加一行「证据结构」**，对应信号分层：「上游 15 条边：供给 5，需求 6，政策 3，同业 1；可回测 5 条；中国侧 3 条；交叉验证 1 条」。它回答 PM 的第二个问题：你这套东西有多硬。

**信号面板对应加「对持仓的增量」一行**：这条信息比新闻多给了什么。比如 BIS 那条：「许可放宽与 25% 关税同日，NVIDIA Q3 指引仍不含中国，说明政策变化尚未转成订单，这个时滞是可观测的验证点」。

**顶部一句话也要带增量**：不写「供需同向」，写「上游供给（中国光模块、云厂 capex）与需求（lab 融资转合同）同向，Q3 看并网兑现与预付到账」。

## 三、数据侧要补的

- 每个指标的「上期值」现在只有 14 条 prior 记录，「变化」列会有一半空。补 2026Q1 的对应值约 20 条，一手可查。
- 独家性标签：边表 notes 里有 e 因子，要抽成一个显式字段，三档：中国侧、交叉验证、公开。
- 证伪条件：每只核心票一到两条，是 D 级判读，我起草你定。
- 比例类计算值，如 $2B 占 $40B 约 5%，标计算值，不算新假设。

你定三件事：区块名「上游读数」还是「AI 链读数」；证伪条件由你写还是我起草；术语翻译表上一轮那张按不按。定了我一起改。

你说得对，那张表把常规数据放在了最前面，AI 发展层反而只剩最后一行。原因是我按边的可回测程度排的序，D1 光模块和 capex 是 T1，前沿是 T3，于是 AI 层被排到了底下。但这个产品的题目是「监测 AI 发展」，面板的第一屏应该是 AI 发展到这只票的因果阶梯，常规数据是阶梯最底下的「产业确认层」，不是主角。

## 一、正确的结构：从 AI 发展往下走到票

对 NBIS 这只 neocloud，AI 发展到它的链是五级，前四级都是 AI 层，第五级才是常规投研：

| 级 | 维度 | 这一级在问 | 现有数据点 | 对 NBIS 的含义 | 可信程度 |
|---|---|---|---|---|---|
| 1 能力前沿 | D5 | 前沿模型还在靠算力堆出来吗 | Epoch 训练算力 5×/年，2020 至今；AA v4.3 榜首 53 并列；ARC-AGI-3 Standard 62.7%，单次评测成本 $26K | 训练与评测算力需求未见饱和 | 方向级 |
| 2 范式与成本 | D5 / D6 | 需求正在从训练转向推理吗，推理单价往哪走 | 开源最高 45 对闭源 53，差 8 分；OpenRouter 月榜 49.8T tokens 榜首是中国开源模型；中低档 API 降价 80%，前沿档一年未降，Gemini Flash 反而公告涨价 | 推理用量扩张，单位价格分档；对 NBIS 是用量向上、但 $20–25M/MW 定价要盯 | 方向级 |
| 3 前沿 lab 的钱与采购 | D3 | 买算力的人有钱吗，真在买吗，买谁的 | 三大 lab 2026 融资 ≈$207B；Anthropic run-rate >$47B；Reflection 融 $25B 后 278 天签 NBIS $1B；Anthropic 2GW MI450 给了 AMD | 需求真实性被证一次；lab 开始多元化供给 | 结构成立，证过一次 |
| 4 AI 政策 | D7 | 算力的供给规则在变吗 | H200 许可放宽与 25% 关税同日；NVIDIA Q3 指引仍不含中国 | GPU 供给与成本的政策变量，尚未转成订单 | 结构成立，待反映 |
| 5 产业确认 | D1 | 上面四级有没有变成在建 | 中际旭创 H1 +182%；四家 capex ≈$166B/季；CRWV backlog $104B | 供给在建强 | 可回测 |
| 票 | | | NBIS 合同电力 5 GW、承诺 >$40B、ARR $3.0B | | 公司数据 |

读法就是增量所在：常规研究员从第 5 级往下读，我们从第 1 级读。第 1 到 4 级决定第 5 级能否延续，所以它们是领先的。这一点用我们已有的数据就能站住，不用编。

## 二、面板怎么放

对象面板的第一个区块就是这个阶梯，名字叫「AI 发展 → NBIS」。每级一行，五列如上，但界面上「维度」列写全词，「可信程度」用「可回测 / 结构成立 / 方向级」三个词。第 5 级和公司数据用浅灰底，视觉上标出「这是常规数据」。这样 PM 一眼看到：上面四级是别人没有系统跟的，下面是大家都有的。

阶梯下面一行「AI 侧证伪条件」，这是真正 AI base 的地方：「若 Epoch 增速回落到 2×以下、开源差距归零且前沿档降价，训练需求逻辑弱化；若 lab 融资转冷或 Reflection 类客户不续签，需求真实性存疑」。现在的证伪条件是 D 级，我起草你定。

**信号面板对应**：一条 AI 发展事件进来，比如 AA v4.3 发布或 OpenAI 降价，「对持仓的增量」那行就是它在阶梯上的位置和往下几级到票：「第 2 级范式与成本 → 推理用量 → NBIS 用量，方向级」。这样每条前沿事件都有落点，不再是孤立的新闻。

## 三、要诚实说的

这个阶梯前四级现在全是方向级，只有第 3 级证过一次。这不是面板的问题，是数据覆盖的问题：D5 有 8 个指标、D6 有 4 个、D2 有 3 个、D4 没有，前沿雷达是 Phase 2。面板把这个薄弱直接显示出来，「AI 发展层 4 条边，方向级；产业确认层 5 条，可回测」，比把常规数据摆前面假装厚实要好。对面试官来说，这也正是「知道自己体系哪里硬哪里软」的证据。

## 四、数据侧要补的

- 给每条边和指标加一个显式的「层」标签：AI 发展层、产业确认层、公司数据层。可以从维度直接派生，D1 是产业确认，其余是 AI 发展层，本体是公司数据。
- 每只核心票写一条 AI 侧证伪条件，D 级。
- 第 2 级现在只有 NBIS 的定价 $20–25M/MW 作为「承压」的对照，没有 neocloud 租赁价序列，那条边是空的，标待填。

这个方向对不对，对的话我按阶梯重做对象面板，术语表一起换。


# 核心判断
这套架构解决了之前「数据类型混排、表格列错位」的**呈现层问题**，但分类逻辑本质还是**数据生产者视角（按信息形状/存储方式分）**，而非**对冲基金信息消费者视角（按决策权重/行动紧迫性分）**。

从信息论和对冲基金实战场景看，核心矛盾是：**信息没有按决策效用做降维滤波，PM的认知信道容量被大量低优先级信息占用，高熵的决策信号被淹没在静态数据里**，整体传递效率还有至少50%的提升空间。

---

## 一、不合理之处（信息论+对冲基金场景双视角）
### 1. L1第一维度缺失「决策紧迫性」，信息熵未做前置降维
#### 信息论原理
信息分类的第一原则是**按接收者的决策效用权重降维**，而非按呈现形式。对冲基金PM的认知信道容量极低（早盘决策时段每秒仅能处理3~5条有效信息），如果输入信息不先做「是否需要立即行动」的滤波，会直接导致信道拥塞，高价值信号被背景噪声淹没。
#### 具体问题
原L1「日历/总览/专页」是**形式维度**，不是**行动维度**：
- 同样在「日历」里，有开盘前必须反应的政策（如出口管制），也有月底才需要跟踪的月度数据；
- 同样在「总览」里，有跌破证伪阈值的持仓风险，也有常规的指标更新。
PM需要自行二次筛选，相当于把信息降维的工作全部扔给用户，单条有效信息的获取成本至少翻倍。
#### 场景痛点
周一早盘5分钟扫盘窗口，PM要在日历里翻20条、再切总览看持仓、再切专页核核心标的，至少3次导航跳转，认知中断严重，很容易漏过关键信号。

### 2. 总览模块平级化设计，违反信息消费的幂律分布
#### 信息论原理
信息访问严格服从幂律分布：80%的使用时长集中在20%的功能上。高频入口的可达性，直接决定系统的平均检索时间（费茨定律：目标越小、距离越远，操作耗时越长）。
#### 具体问题
总览下的「持仓/指标/边」三个子项平级摆放，但使用频率、信息权重完全不对等：
- 持仓：PM日均访问10~20次，是核心交易工作区
- 指标：日均2~3次，研究复盘用
- 边（映射关系）：日均0~1次，深度梳理用

平级设计让最高频的持仓入口没有视觉权重，每次都要重新定位点击；更关键的是，**「边」本质是实体的关联属性，不是独立的对象类型**，和持仓、指标平级会造成信息实体的重复定义，底层数据冗余。

### 3. 专页粒度不统一，信息层级出现认知断层
#### 信息论原理
同一层级的信息节点，粒度应当保持一致，否则会破坏用户的认知预期，每次切换都要付出上下文适配的开销。信息粒度的突变，是隐形的效率杀手。
#### 具体问题
专页L2同时包含三类完全不同粒度的对象：
- 宏观级：AI发展全景（整个产业链）
- 中观级：单只标的（NBIS/RBRK等个股）
- 组合级：整体组合、尾仓

三者的信息密度、结构、使用场景完全不同：AI全景是行业研究用，个股专页是交易决策用，组合页是风控用。放在同一个「专页」分类下，相当于把「全国地图/街道详图/小区平面图」都放进「地图」一个文件夹，每次打开都要重新调整认知粒度。

### 4. 「图是表的换肤」浪费了可视化的信息压缩增益
#### 信息论原理
可视化的核心价值是**信息压缩与模式识别**：通过空间编码把高维数据降维，让人脑并行处理，信息密度可比表格提升一个数量级。如果只是把表格数据换个形状画出来，属于冗余编码，没有任何信息增益。
#### 具体问题
原设计中，映射图=边表的图模式、阶梯图=指标表的图模式，本质只是呈现形式切换，没有做信息抽取：
- 边表35条链路，图上还是35条边，PM还是要自己找哪条链路出了异常；
- 指标75个节点，阶梯图还是75个节点，PM还是要自己找变化大的。

可视化没有发挥「信噪比增强」的作用，还是静态结构图，不是信号热力图。
#### 场景痛点
PM看映射图是为了1秒找到「哪个环节传导断了、哪个节点超预期」，而不是看全量静态关系。现在的图只是换了个展示方式，没有帮用户做任何信息过滤。

### 5. 缺失「异常信号」独立信道，整体信噪比不足
#### 信息论原理
对于连续监控场景，**变化量/异常量的信息熵远大于静态量**。对冲基金的核心需求是「什么变了」，而不是「是什么」。没有独立的信号通道，等于把峰值信号和背景噪声放在同一个信道传输，接收端必须自己解调，效率极低。
#### 具体问题
原架构的四种信息形状里，没有「预警/异常」这一类。所有超预期、破阈值、证伪信号，都散落在日历、总览、专页里：
- 比如NBIS并网低于预期、Reflection融资不及预期这些关键证伪信号，混在20条日历条目里，PM很容易漏过；
- 系统只做了「信息存储分类」，没做「信息抽取和优先级推送」，本质还是资料库，不是决策辅助系统。

### 6. 下钻页信息无分层，认知中断成本高
#### 信息论原理
下钻路径的信息应该是**增量式、按需展开**，而不是一次性全部倾倒。用户点击下钻的目的非常明确（验证某个数据、查证伪条件、看来源），全量堆砌会增加信息搜索时间，直接打断决策流。
#### 具体问题
原设计把P级、分工、可得性、假设、证伪条件全部下沉到下钻页，但没有分层。PM点进去只想看「这个数据的证伪阈值是多少」，结果要在一堆运维字段里搜索，认知中断超过3秒，决策连续性就会被破坏。

---

## 二、可优化方向（信息效率最大化）
### 1. L1重构：按「决策优先级」做第一信道分层
把L1从「形式分类」改成「**决策场景+形式**」双轴，优先按紧迫性给信道分流：
- **第一级（高速信道）：信号中心**——所有破阈值、证伪、超预期的异常信息自动聚合，开盘前必看，红色强提示
- **第二级（中速信道）：日历**——按时间排序的事实流，可按类型/紧迫性筛选
- **第三级（工作区）：持仓总览**——默认首页，占据最大视觉权重
- **第四级（低速信道）：研究库**——指标、产业链图、行业专页，低频深度研究用

> 逻辑：高优先级信息走高速通道，低优先级走低速通道，避免信道拥塞，完全对齐对冲基金「先处理紧急信号→再看日常数据→最后做研究」的决策流。

### 2. 总览做权重分级，把「关系」还原为实体属性
- 持仓总览升级为默认主页面，指标、边作为持仓的附属标签页，而非平级入口；
- 「边（映射关系）」不再作为独立总览项，而是内嵌在持仓行和专页中：持仓行hover显示上下游链路，专页内画专属链路图。

> 逻辑：服从幂律分布，把最高频入口的可达性拉满；消除实体重复定义，关系归属于实体，而不是独立成实体。

### 3. 专页按粒度分层，统一信息密度
把专页拆成三个明确粒度的层级，避免认知断层：
- 组合层：组合总览、风险敞口、收益归因
- 标的层：单只股票专页（NBIS/RBRK等）
- 行业层：AI发展全景、产业链总览

> 逻辑：同一层级粒度一致，用户认知预期稳定，减少上下文切换成本。

### 4. 可视化从「换肤」升级为「信息压缩」
每张图都做**信号增强**，而非数据复刻：
- 映射图：默认只高亮「状态异常」的链路（需求转弱、产能延期、证伪临近），正常链路灰度弱化；用颜色深浅传导变化幅度
- 阶梯图：默认只显示本季度变化超±20%的节点，正常节点折叠
- 持仓图：用热力图显示权重和风险敞口

> 逻辑：把35条边的全量数据，压缩成3~5条异常边的信号，信息密度提升7~10倍，人脑可以毫秒级识别模式。

### 5. 新增「信号中心」，做主动信息抽取
单独做信号面板，自动聚合所有高熵异常，按优先级排序：
- 风险信号：证伪条件触发、持仓跌破预警线（最高级）
- 基本面信号：财报超预期、backlog/并网破阈值
- 事件信号：政策变动、大额融资、核心合同签约
- 每条信号直接挂载跳转入口：一键跳对应专页、对应日历条目

> 逻辑：把信息从「按类别存储」变成「按优先级推送」，PM只需要处理经过抽取的高熵信息，信噪比提升一个数量级。

### 6. 下钻页增量分层，按需展开
下钻页分三层折叠，默认只展首层：
- **首层（决策层）**：数值、变化、证伪条件、核心结论
- **二层（支撑层）**：来源、发布日期、核心假设
- **三层（运维层）**：内部分工、P级、可得性、内部ID

> 逻辑：按需加载信息，用户每次只看到当前需要的粒度，减少搜索时间，保持决策流不中断。

---

## 总结
这套架构的底层数据同源、实体关系清晰，已经解决了最基础的结构问题；下一步的核心优化方向是**从「数据组织视角」彻底转向「决策消费视角」**，用信息论的**降维、分层、滤波、压缩**四大手段，完全对齐对冲基金的决策节奏，最终目标是让PM单位时间内获取的有效决策信息最大化。

这份意见里有一半我认同，有一半是站不住的。逐条说，然后给合并后的方案。

## 逐条判断

| 意见 | 判断 | 理由 |
|---|---|---|
| 1 & 5 · 缺「信号」信道，L1 应按紧迫性分 | **对，但要打折** | PM 第一个问题确实是「什么变了」。但它说的「破阈值 / 证伪触发」我们**做不出来**：regime 阈值（C6）、证伪条件都是 D 级未定，🟠 不进阈值是我们自己立的规矩。能诚实给的信号只有三种：有方向判断的新观测（A11，B 级）、对公司指引 / 目标的偏差（有指引才算）、临近的排期。它举的「NBIS 并网低于预期、Reflection 融资不及预期」在我们的 173 条记录里不存在——Reflection 是 $25B pre 关闭，NBIS 并网只有目标没有实测。信号必须从记录里长出来，不能编 |
| 2 · 持仓应是默认页 | **对** | 频率上持仓 ≫ 指标 ≫ 边，右栏 watchlist 已经是这个判断 |
| 2 · 「边不是实体，是属性」 | **错** | 边有自己的字段（跳数、可信层、可交易 / 预警分、中间节点），是图模型里的一等实体；塞回两端实体会重复存。UI 上可以从票行 hover 看链，数据层不动 |
| 3 · 专页粒度混（行业 / 标的 / 组合） | **对** | 同一菜单里混了三种粒度，改分组就行 |
| 4 · 图只是表换肤 | **半对** | 映射图已有按票聚焦和命中高亮，但默认画全量确实没做信噪比。可以默认只亮「近 60 天有方向判断」的边、其余灰——这用现有数据就能算。它说的「只显示变化超 ±20% 的节点」不行：上期值只有 1/3 有，而且 20% 是又一个没登记的阈值 |
| 6 · 下钻页分三层折叠 | **对，便宜** | 现有区块重排：决策层 / 支撑层 / 运维层，默认只展第一层 |
| 「每秒 3–5 条」「提升 50%」「密度 7–10 倍」 | 无依据 | 略过 |

它的底层批评——「按形状分是生产者视角」——我部分接受：形状是导航的正确单位，但**落地页**应该按决策流排，而不是按形状排。两者不冲突：L1 按 PM 早上的顺序排列，每一项内部仍是单一形状。

## 合并后的 L1

```
信号  ·  日历  ·  持仓  ·  研究                 搜索 (/)   as-of 09-19   ◐
```

**信号**（默认落地页，事实 × 优先级）— 三组，按 影响持仓权重 × 边星级 排序，每条带跳转：
- 方向变化：近 7 天有 ↑ ↓ ! 判断的观测
- 对指引偏差：实测 vs 公司指引 / 官方目标，有指引才出现
- 临近排期：未来 14 天，非公告日期标「估计」
- 底部一行灰字：证伪条件 3 条（B 稿）· 阈值待定（C6）· 未触发 / 不可判——把做不到的写出来，这本身是方法论

**日历**（事实 × 时间）— L2 行类型：本周 · 数据 · 财报 · 事件·政策 · 前沿。

**持仓**（对象 × 属性）— 表按权重排，行 hover 显示上游链；L2：组合 · NBIS · RBRK · STAA · FCEL · 尾仓 → 票专页（链图只画本票）。

**研究**（低频深度）— AI 发展专页（阶梯 + 读数 + 过闸动态 + 全图，默认只亮活跃边）· 指标库 · 边表（表 ↔ 图）· 术语 · 假设。

下钻页统一三层折叠：决策层（值 / 变化 / 方向 / 证伪条件 / 一句结论）· 支撑层（来源 / 期间 / 假设 ID）· 运维层（分工 / P 级 / 可得 / ID）。

## 代价

信号页是新的（约 1.5 h，全部用现有派生字段），L1 重排 + 日历五视图 + 持仓表 + 研究页归并约 3 h，下钻分层 0.5 h，映射图默认降噪 0.5 h。共 5–6 h，今晚可以做完，明天上午编译交付物 ① ③。

一件事要你定：信号页**不做**红色「破阈值」类提示，只做上面三组 + 一行诚实说明。你如果想要阈值类信号，得先定 C6 和三条证伪条件的数——那是 D 级，你定了我就接。

确认了我开始，这份意见和取舍记 E45。

全文已写到 [18-信息架构规格.md](18-信息架构规格.md)，下面是完整内容。原则一句话：L1 按 PM 一天的决策顺序排，每个 L1 内部只有一种信息形状；L2 = 同一形状下「行是什么」不同；所有派生只用现有五张表 + 13F，窗口与排序分是设计选择（🔵 仅排序，不进阈值）。

## L1-1 信号（默认落地页）

形状：事实 × 优先级。场景：周一早盘 5 分钟，「上周什么变了、这周要盯什么」。为什么在 L1：PM 第一个问题，而且不是日历的子集——日历按时间排，这里按「对我的书重要程度」排，行序不同。

| L2 | 场景 | 点进去 | 推断逻辑 |
|---|---|---|---|
| 方向变化 | 上周哪些观测在推 / 拆我的 thesis | 日期 · 方向 chip · 实体 · 一句话事实（≤ 60 字）· 打到哪些票（权重）· 来源级（非一手才显示）· 跳转 | 观测记录中 direction ∈ {↑,↓,!} 且 0 ≤ as-of − knowledge_time ≤ 7 天（可切 30）；排除 prior / position / computed。**排序分 = Σ 该指标出发的边（到票的 13F 权重 × 边星级 ★1–3）**，票自身指标按 ★3；同分按时间降序、一手优先。排序分登记 A12 🔵 |
| 对指引偏差 | 谁超了 / 没达到公司自己说的数 | 实体 · 期间 · 实测 · 指引 · 偏差 % · 方向 · 跳转 | 同指标同实体同期间下同时有 actual 与 guidance/target 记录；都是数字才算 (实测 − 指引)/指引，指引区间取中点（A14）；不可解析只并排不算 %。按 \|偏差\| 降序。没有指引的不出现——不用一致预期，因为没有一手一致预期源 |
| 临近排期 | 这两周有什么要来 | 日期（距今 n 天）· 事件 · 日期依据（非公司公告才标）· 打到哪些票 | 日历 dated 且 0 < date − as-of ≤ 14 天，升序；依据取 basis 列（C9） |
| 底部一行 | 诚实说明 | 证伪条件 3 条（B 稿）状态一律「不可判：阈值待定（C6）」；regime「同向偏多」标 doc 28 占位 | 不做「破阈值」信号：阈值 D 级未定，🟠 不进阈值 |

## L1-2 日历

形状：事实 × 时间。场景：按时间线扫「什么时候来、刚发生了什么」。为什么在 L1：时间是第二个坐标，且每种行类型列不同，需要自己的一级空间。

行类型规则 = **记录类型 × 节点角色 × 层**，58 行全部落位：

| L2 | 规则 | 行数 | 列 | 对标 |
|---|---|---|---|---|
| 本周（默认） | 全部类型，本周 ± 7 天 | — | 日期 · 类型 · 名称 · 要点（按类型换形式）· 对持仓 | 混合时间线 |
| 数据 | actual/prior/guidance/computed × 领先指标 × 产业确认层 | 15 | 层 · 地区 · 重要 · 指标 · 实测 · 预期/指引 · 上期 · 变化 · 对持仓 | TV Economic |
| 财报 | actual/guidance/prior × {公司数据, 仓位} × 公司数据层 | 18 | 票（权重）· 期间 · 营收 实测/指引/上期 · YoY% · 关键 KPI · 语气 · 下次 | TV Earnings |
| 事件·政策 | event/state × {事件, 政策, 公司里程碑} | 16 | 类型 · 事件 · 发生了什么 · 相关方 · 状态 · 对持仓 · 来源级（非一手才显示） | 事实型无数值列 |
| 前沿 | {前沿, 观点} 或（领先指标 × AI 发展层，如定价） | 9 | 日期 · 类型 · 主体 · 主张 · 来源级 · 判断（D 级，未判显示待判）· 对持仓 | 我们独有 |

列的推断：实测/预期/上期直接取当日记录，预期只取 guidance/target；**变化** = 有上期且都是数字才算 YoY，有指引算 vs 指引 %，否则「—」（现有上期值约 1/3，不造）；方向 chip = 记录级 A11；重要 = 该节点出发边的最高星级，无边则 D1/D3/D7 = H、D5/D6 = M、其余 L（A1/A2）；页内筛选条 = 时间 · 层 · 影响持仓 · 地区 · 搜索。

## L1-3 持仓

形状：对象 × 属性。场景：「我的票现在什么状态」，日均访问最高。L2 = 组合 · NBIS · RBRK · STAA · FCEL · 尾仓。

持仓表列的推断（全部是现有 build 逻辑）：

| 列 | 推断 |
|---|---|
| 权重 | 13F 2026Q2 position 记录 |
| 上游供给 / 下游需求 / 政策环境 | 60 天内该票 供给边(+自身) / 需求边 / 政策边 的来源指标记录方向聚合：**↓ 当 n↓ ≥ max(n↑,1)；否则 ↑ 当 n↑>0；否则 →；有 ! 追加 !** |
| 本周 | 7 天内、有 ★ 的边或自身指标、方向 ≠ → 的记录，按星级再按时间取第一条 |
| 风险 | 60 天内在「预警占优边 ∪ 政策边 ∪ 竞对边 ∪ 自身」上 ! 或 ↓ 的记录数，自身有口径警示（合同电力≠并网）再 +1 |
| 领先 | 90 天内 T1/T2 供需边聚合方向，标最高层级 |
| 下一个数据点 | 该票相关指标最近一条未来日历行，优先财报/目标/截止，带距今天数与依据 |

行 hover = 该票上游链（边表 to_ticker = 票，按侧别分组）。

票专页三层折叠：**决策层**（status_line · 权重 · 供/需/政 三色块 · 综合判断表盘（定性，C6 未定）· 本周 · 证伪条件）/ **支撑层**（阶梯 · 公司数据字段表 · 证据结构计数 · 120 天时间条 · 关键事实简报）/ **运维层**（观测记录表 · P 级 · 分工 · 可得 · 假设 ID）。

阶梯推断：级序 D5 → D6 → D3 → D2 → D4 → D7 → D1 → 公司数据；每级取「到本票、来源在该维度」的边，状态 = 窗口内方向聚合（AI 层 120 天、产业/公司 90 天），可信 = 该级最高层（T1 > T2 > T3），独家性 = 边标签，附最新两条点；中间节点是 A10 结构先验。

组合页：13 只权重与操作、总值、换手 80.30%、核心/非 AI/尾仓分组、前四集中度、13F 下次截止（C13）。**没有收益归因**——无行情源，不做。

## L1-4 研究

形状：混合，低频深度（TV 的 More）。场景：复盘、梳理链路、核来源。为什么在 L1：必须可达，但不占高频入口。

| L2 | 场景 | 点进去 | 推断逻辑 |
|---|---|---|---|
| AI 发展专页 | 「AI 走到哪一级了」，扣题之页 | 读数带 4 格（AA 指数 · 训练算力增速 · 开源差距 · cost-per-token，≥3 点画折线）· **全局阶梯** · 过闸动态（近 90 天）· 顶会与发布排期 · 全图 · 缺口灰字（论文斜率待建 · 新范式待填 · D4 合规） | 全局阶梯 = 每级取该维度出发的全部边（不限票）聚合；**活跃边 = 来源指标 60 天内有 ↑↓! 记录（A13）**，全图默认只亮活跃边、其余灰 |
| 指标库 | 盯什么、从哪来、缺什么 | 75 节点表 + 层/维度树 + 筛选 | 直接取；D1–D7、P 级、A–D 只在这里出现 |
| 边表 | 这条链多可信、几步、独家吗 | 35 条 + 右上 表 ↔ 图（图 = 映射图） | 可交易/预警占优 = 可交易分 ≥ 预警分（A9 🔵）；步数 = 中间节点 + 1（A10） |
| 术语表 | 界面词 ↔ 内部词 | 27 条 | 直接取 |
| 假设台账 | 系数从哪来、什么状态 | A/B/C/R 状态机 | 目前在 md，待接入 |

## 派生字段总表

| 字段 | 规则 | 窗口 | 级别 / 假设 |
|---|---|---|---|
| 方向 | 人/机判断，相对所喂票 thesis | — | B · A11 |
| 链状态 供/需/政 | agg：↓ 优先 → ↑ → →，! 追加 | 60 d | 设计选择 |
| 本周 | 非 → 的最高星级记录 | 7 d | 设计选择 |
| 风险计数 | ! 或 ↓ 计数 + 口径警示 | 60 d | 设计选择 |
| 领先读 | T1/T2 供需边 agg | 90 d | 设计选择 |
| 重要性 | 三档 | — | A1/A2 🔵 |
| 信号排序分 | Σ 权重 × 星级 | — | **A12 🔵 新** |
| 对指引偏差 | (a−g)/g，区间取中点 | 同期间 | **A14 新** |
| 活跃边 | 60 d 内有 ↑↓! | 60 d | **A13 🔵 新** |
| 阶梯 | 按维度分级聚合 | 120/90 d | A10 |
| 可交易/预警占优 | 可交易分 ≥ 预警分 | — | A9 🔵 |
| 综合判断 regime | NBIS「同向偏多」是 doc 28 占位 | — | **D · C6 未定** |

三个新假设（A12–A14）我会登记到假设台账；regime 那个占位会在界面上明说。有不同意的地方直接改这份 md，我按它建。
